# 个人主页 · 版本与修改记录

> 本文件用于同步记录「个人主页」各版本的改动内容。
> 当前版本页面文件：`outputs/homepage/index.html`

---

## 版本管理约定

- 每次更新主页时，在 `outputs/homepage/`（桌面「个人主页仓库」文件夹）中**保留每一个版本的完整文件**，不覆盖旧版本。
- 版本文件命名规则：`v1.0.html`、`v1.1.html`、`v2.0.html` ……，新版本直接以递增版本号生成新文件。
- `index.html` 始终指向**最新版本**，方便直接预览。
- 每个版本在本文件追加一条记录，标明改动内容。
- 历史版本同时通过 Git 提交（commit）保留，双保险。

---

## v3.5（当前）

**改动：「关于我」新增电子邮箱**

- 「关于我」信息卡新增「电子邮箱」一行：`2421757972@qq.com`（可点击发送邮件）。
- 新增 `.info-value a` 链接样式，与主题配色一致。

> 已归档为独立文件：`outputs/homepage/v3.5.html`（`index.html` 同步指向本版本）。

---

## v3.4

**改动：在线版新增「下载完整版」入口**

- 在线静态版页面底部新增「下载完整版 · 本地运行」按钮，点击下载项目 zip（含 `启动主页.bat`）。
- 该入口仅在静态托管（GitHub Pages）环境显示，本地版不显示。

> 已归档为独立文件：`outputs/homepage/v3.4.html`（`index.html` 同步指向本版本）。

---

## v3.3

**改动：适配 GitHub Pages 静态部署**

- 反馈表单改为前端直连 Supabase `feedback` 表（`POST /rest/v1/feedback`），在线静态版与本地版行为一致，反馈统一写入云端。
- 在线静态版（GitHub Pages）检测到无 Python 后端时，AI 聊天自动降级为内置规则回复，并在聊天区与页脚给出「AI 对话需本地运行」的提示。
- 前端仅携带 Supabase 公开 anon key（最小权限，仅可插入），DeepSeek Key 仍只保存在本地 `.env`，不上传。

> 已归档为独立文件：`outputs/homepage/v3.3.html`（`index.html` 同步指向本版本）。

---

## v3.2

**改动：移除头像**

- 移除英雄区的头像（`avatar.jpg`），顶部只保留「名字 + 一句话介绍」。
- 删除 `.avatar` 与 `.avatar img` 相关样式及头像 DOM。
- `avatar.jpg` 文件仍保留在目录中，但页面不再引用。

> 已归档为独立文件：`outputs/homepage/v3.2.html`（`index.html` 同步指向本版本）。

---

## v3.1

**改动：反馈数据接入 Supabase 云数据库**

- 反馈从「追加写入本地 `feedback.jsonl`」升级为「写入 Supabase 云数据库」，多设备访问时反馈集中到云端，不再分散在本机文件里。
- 后端 `server.py` 的 `POST /api/feedback` 改为优先写入 Supabase `feedback` 表（自动记录 `created_at` 提交时间与 `version` 版本）。
- Supabase 采用最小权限：匿名访客仅能「插入」反馈，无法读取/修改/删除；数据查看走 Supabase dashboard。
- Supabase 写入失败时自动回退到本地 `feedback.jsonl`，确保不丢反馈。
- `.env` 新增 `SUPABASE_URL` 与 `SUPABASE_ANON_KEY`（已由 `.gitignore` 忽略，不提交）。

> 已归档为独立文件：`outputs/homepage/v3.1.html`（`index.html` 同步指向本版本）。

**补充修复（v3.1）：支持手机局域网访问**

- `server.py` 服务绑定地址由 `127.0.0.1` 改为 `0.0.0.0`，手机可通过局域网 IP 访问主页与头像（`127.0.0.1` 仅本机可访问，此前手机端头像加载失败）。
- 启动时新增打印「局域网访问：http://<本机IP>:端口」提示。

---

## v3.0

**改动：新增页面反馈入口**

- 页面右下角新增「反馈」固定按钮，点击原地弹出简短表单，不跳转页面。
- 表单字段：你的名字、与我的关系、本条反馈针对的设备、反馈内容（必填）。
- 弹窗内明确提示「反馈内容不会公开，仅周亦乐本人可见」。
- 后端新增 `POST /api/feedback`，自动记录提交时间与页面版本，追加写入 `feedback.jsonl`（已加入 `.gitignore`，不提交、不公开）。

> 已归档为独立文件：`outputs/homepage/v3.0.html`（`index.html` 同步指向本版本）。

---

## v2.3

**改动：API Key 收敛到后端 + 头像固定为太极图**

- API Key 从前端彻底移除，仅保存在后端 `.env`（由 `.gitignore` 忽略，不提交）。
- 前端聊天改为调用后端 `/api/chat` 代理，不再直连 DeepSeek、不再携带 Key / Base URL / 模型名。
- 移除前端「设置」面板（API Key / Base URL / 模型名 输入框及保存/测试/清除按钮）。
- 数字分身人设提示词统一收敛到后端 `server.py`。
- 头像固定为水墨太极图（`avatar.jpg`），移除「点击换头像」上传功能。

> 已归档为独立文件：`outputs/homepage/v2.3.html`（`index.html` 同步指向本版本）。

---

## v2.2

**改动：精简主页视觉元素**

- 移除英雄区「周亦乐」名字旁的「v2.0」蓝色胶囊版本标签。
- 移除数字分身标题右侧的红黄绿三色状态徽章（本地演示 / 已配置·未验证 / 大模型已接入）。

> 已归档为独立文件：`outputs/homepage/v2.2.html`（`index.html` 同步指向本版本）。

---

## v2.1

**改动：数字分身聊天加入流式打字效果**

- 后端 `server.py` 改为 DeepSeek 流式转发（SSE 增量逐段写回）。
- 前端聊天区改为 `fetch` 流式读取，回复逐字显示，像真人打字。
- 保留多轮上下文与「正在思考…」过渡提示。

> 已归档为独立文件：`outputs/homepage/v2.1.html`（`index.html` 同步指向本版本）。

---

## v2.0

**改动：数字分身接入 DeepSeek 真实模型**

- 新增后端服务 `server.py`：托管页面 + 提供 `/api/chat` 接口，代理调用 DeepSeek `deepseek-chat` 模型。
- 数字分身人设写入系统提示词：大学生周亦乐，近期关注 AI 应用 / 内容表达 / 知识整理，语言幽默、思维跳脱但富有逻辑、回答简洁像人话、擅长把复杂问题讲清楚。
- 聊天区从「关键词规则回复」改为调用真实模型，并保留多轮上下文。
- 新增 `.env` 存放 API Key（已被 `.gitignore` 忽略，不会提交）；新增 `.gitignore`。

---

## v1.1

**改动：隐藏「点击换头像」提示标签**

- 移除了头像下方显示「点击换头像」的小标签及其样式。
- 保留「点击头像即可更换头像」的功能（点击头像仍会弹出本地文件选择框）。

> 已归档为独立文件：`outputs/homepage/v1.1.html`（`index.html` 仍指向本版本）。

---

## v1.0（第一版原型）

**改动：根据需求生成首个可预览版本**

按需求文档实现以下内容：

- **顶部英雄区**：头像 + 名字「周亦乐」+ 一句话介绍 + 「大学生」身份标签。
- **个人信息展示区**：4 张卡片，包含「现在主要在做 / 兴趣 / 记忆点 / 最近在做的事」。
- **数字分身聊天区**：气泡式对话窗口 + 3 个快捷提问按钮 + 关键词规则问答。
- **头像自定义**：点击头像可上传本地图片替换。
- **风格落地**：深色为主、黑白灰蓝配色、简约科技感、适配手机。
